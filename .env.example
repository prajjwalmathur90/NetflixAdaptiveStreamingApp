# Shared media storage (outside repo — mount this in Docker for workers & API)
MEDIA_ROOT=

# API
PORT=
DATABASE_URL=
TEMPORAL_ADDRESS=
TEMPORAL_TASK_QUEUE=
CORS_ORIGIN=

# Worker
FFMPEG_PATH=
FFPROBE_PATH=

# Web (Vite)
VITE_API_URL=
